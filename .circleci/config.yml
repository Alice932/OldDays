version: 2.1
jobs:
  build:
    docker:
      - image: circleci/php:8.1
    steps:
      - checkout
      - run:
          name: Install PHP dependencies
          command: composer install
      - run:
          name: Run tests
          command: phpunit
